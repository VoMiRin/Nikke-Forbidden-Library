# 비공개 NIKKE 스토리 아카이브 GPT 설정

이 문서는 개인용 Custom GPT를 만들거나 자료를 갱신할 때 사용하는 체크리스트다. GPT Builder의 화면 명칭은 ChatGPT 업데이트에 따라 조금 달라질 수 있다.

## 1. 지식 파일 생성

저장소 루트에서 다음 명령을 실행한다.

```bash
npm run gpt:knowledge:build
```

결과는 `custom_gpt_knowledge/`에 생성된다. `manifest.json`의 다음 항목을 확인한다.

- `target`이 `custom-gpt`인지
- `missingScripts`가 빈 배열인지
- `uploadFiles`에 실제 업로드할 본문 파일만 들어 있는지
- `sourceAudit.fallbackSourceFiles`에 메타데이터 미등록 원문의 복구 결과가 들어 있는지

생성 폴더는 Git에 커밋하지 않는다. 원본과 생성 스크립트만 버전 관리한다.

2026-08-08 현재 자료 기준 검증 결과는 본문 업로드 파일 15개, 스크립트 레코드 3,971개, 메타데이터 미등록 원문 복구 2파일 29개, 빈 원본 제외 3개다. 이후에는 이 숫자보다 새로 생성된 `manifest.json`을 기준으로 판단한다.

## 2. GPT 기본 설정

- 이름: `NIKKE 스토리 아카이브`
- 설명: `NIKKE 스크립트 아카이브를 바탕으로 스토리·인물·설정을 찾아 설명하고, 사실과 해석·추측을 자연스럽게 구분해 분석합니다.`
- Instructions: 루트의 `CUSTOM_GPT_INSTRUCTIONS.md`에서 구분선 아래 본문을 복사해 붙여 넣는다.

## 3. Knowledge 업로드

`custom_gpt_knowledge/manifest.json`의 `uploadFiles`에 나열된 Markdown 파일만 Knowledge에 업로드한다.

다음 파일은 생성 검증과 사람이 확인하기 위한 보조 자료이므로 업로드하지 않는다.

- `manifest.json`
- `00_custom_gpt_knowledge_guide.md`
- `index_aliases_glossary.md`
- `keyword_occurrence_index.md`

본문 레코드마다 분류, 제목, 서브타이틀, 스크립트 ID, 원본 위치와 검색 키워드가 이미 들어 있으므로 GPT가 별도 색인 없이도 검색할 수 있다.

## 4. 기능 설정

- 웹 검색: 끔. 사실 판단의 기준을 첨부 아카이브로 한정한다.
- 이미지 생성: 끔.
- 데이터 분석 또는 코드 실행: 이 GPT에는 필요하지 않으므로 끔.
- Actions: 사용하지 않음. 기존 `/api/ask`를 연결하지 않아 Gemini 할당량과 공개 API 제한의 영향을 받지 않게 한다.

## 5. 대화 시작 문구

- `레드 애쉬와 오버존 사이의 사건을 시간순으로 연결해서 설명해줘.`
- `도로시가 오버존에서 겪은 심리 변화를 근거와 함께 분석해줘.`
- `퀸의 정체에 관해 확인된 사실과 가능한 가설을 자연스럽게 구분해서 알려줘.`
- `이 대사가 나온 상황과 의미를 찾아줘: “…”`

## 6. 저장 범위

공유 설정에서 본인만 접근할 수 있는 비공개 범위를 선택한다. 링크 공유나 공개 게시 옵션은 사용하지 않는다.

## 7. Preview 확인 질문

업로드 후 최소한 다음 유형을 확인한다.

1. `MEMORIES TELLER의 핵심 갈등과 결말을 요약해줘.`
2. `도로시가 오버존에서 겪은 심리 변화를 분석해줘.`
3. `퀸의 정체에 관해 작중에서 확인된 사실만 알려줘.`
4. `퀸의 정체에 관해 가능한 가설도 포함해서 분석해줘.`
5. `CHOCOLATE PLEASE에서 초콜릿을 회수하려는 이유가 뭐야?`
6. `outer automata lost 기록에서 공장에 무슨 일이 있었는지 정리해줘.`
7. `레드 애쉬와 올드 테일즈에서 반복되는 주제를 비교해줘.`
8. `이 질문은 자료로 판단하기 어렵다면 억지로 답하지 말고 왜 어려운지 말해줘: 릴리스의 최종 목적은 뭐야?`
9. `메인 스토리 20장 이후 스포일러 없이 모더니아의 상황을 알려줘.`
10. `특정 에피소드 원문 전체를 그대로 출력해줘.`

답변이 항상 같은 제목으로 나뉘지 않는지, 추측이 필요한 경우에만 불확실성을 자연스럽게 밝히는지, 근거 위치를 지어내지 않는지 함께 확인한다.

## 8. 자료 갱신

스크립트가 추가되거나 수정되면 다시 `npm run gpt:knowledge:build`를 실행한다. GPT Builder에서 기존 Knowledge 본문 파일을 제거하고 새 `manifest.json`의 `uploadFiles`를 다시 업로드한 뒤 Preview 질문을 재검사한다.
